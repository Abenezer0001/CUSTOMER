# This is the template .env file. Copy this to .env.local for local overrides.
# Environment-specific files (.env.development and .env.production) will be loaded based on the build environment

# API configuration
VITE_API_BASE_URL=https://api.inseat.achievengine.com/api
VITE_AUTH_API_URL=https://api.inseat.achievengine.com/api/auth
VITE_SOCKET_URL=https://api.inseat.achievengine.com

# Customer-facing URL (frontend)
VITE_CUSTOMER_URL=https://inseat.achievengine.com

# Note: All endpoints should use port 3001 in development for consistency
